# Editor access to user list and password reset (deferred)

Editors may currently list all users and trigger password reset for any user (`requireEditorOrSuperAdmin`). Restricting these to Super Admin only was considered and deferred; leave behavior unchanged for now. Revisit when tightening identity-admin powers separately from DApp operations.
